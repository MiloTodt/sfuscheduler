<%= form_for(@build_schedule) do |f| %>
  <% if @build_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@build_schedule.errors.count, "error") %> prohibited this build_schedule from being saved:</h2>

      <ul>
      <% @build_schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :courses %><br>
    <%= f.text_area :courses %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
